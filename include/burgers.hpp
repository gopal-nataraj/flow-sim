int inviscidBurgers1d(const double* u_0x,
                      const int& nt,
                      const int& nx,
                      const double& dt,
                      const double& dx,
                      const double& tol,
                      double* u);