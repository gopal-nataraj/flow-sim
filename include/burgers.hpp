int inviscidBurgers1d(const double* u_0x,
                      const double& nt,
                      const double& nx,
                      const double& dt,
                      const double& dx,
                      const double& tol,
                      double* u);